package ccGame2;

// Could add other variables as ideas are implemented. One such example would be a hasFlare() that would return a boolean saying whether a character has a flare.
// Perhaps hard pirates have these flares and in the pirate2 class I could have some behavior associated with the flare occur for the hard pirate and not the others.


public interface Difficulty{
	int getSightRange();
}
